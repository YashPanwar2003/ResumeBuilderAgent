services:
  - type: web
    name: resume-career-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    plan: free
    envVars:
      - key: GOOGLE_API_KEY
        sync: false  # You will set this manually in Render Dashboard
